import getGeometry, { isVestigingAmsterdam } from './geometry'

describe('geometry', () => {
  describe('isVestigingAmsterdam', () => {
    it('returns true', () => {})
    it('returns false', () => {})
  })

  // it('', () => {})
})
