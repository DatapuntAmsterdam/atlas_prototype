import fixtureSingle from './typeahead.json'
import fixtureSingleAuth from './typeahead_auth.json'
import { TypeAhead } from './types'

export const singleFixture = fixtureSingle as TypeAhead[]
export const singleFixtureAuth = fixtureSingleAuth as TypeAhead[]
export const path = 'typeahead'
export const fixtureId = null
