const plugins = (on: any) => {
  // on('before:spec', () => {
  //   console.log('before:spec')
  //   cy.window().then(() => {
  //     console.log('before:spec cy.window()')
  //     // @ts-ignore
  //     // eslint-disable-next-line
  //     const { interceptApiFixtures } = require('../support/api')
  //     interceptApiFixtures()
  //   })
  // })
}

export default plugins
